/**
 * Interface que se encarga de definir la información sobre la ubicación de una EDS
 */
export interface InformacionDeUbicacionEds {
    direccion: string;
    latitud: string;
    longitud: string;
}