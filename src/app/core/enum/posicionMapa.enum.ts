/**
 * Enumerable que tiene las ubicaciones predeterminadas en el caso de que no se permita al dispositivo acceder a la ubicación
 */

export enum EPosicionMapa {
    latitudDefault = 10.96854,
    longitudDefault =  -74.78132
}