export class ObjPage {
    // The number of elements in the page
    size = 4;
    // The total number of elements
    totalElements = 0;
    // The total number of pages
    totalPages = 0;
    // The current page number
    pageNumber = 0;
    // The standardized messages of the table
    messages = {emptyMessage:'No hay registros para mostrar', totalMessage: 'total'};
}
